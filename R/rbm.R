#' @export
newModel.rbm <- function(dataset, class_col = length(dataset)) {
  if (!requireNamespace("darch", quietly = TRUE)) {
    stop("dlvis: Package 'darch' is not installed and is needed for Restricted Boltzmann Machine functionality")
  }

  
}
